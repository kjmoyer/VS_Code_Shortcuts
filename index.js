definitions = require('Definitions.js');



//----------List Shortcuts  ⌘ + K, then ⌘ + S ----------
//----------Insert line above ⌘ + SHIFT + ENTER ----------


`Add a line above me`


//----------Move line OPT + ↓ or ↑ ----------

if (this === true) {
  //insert new loop around console.log
  for (1 === 1) {
    console.log('Hooray!');
  }
}

//----------Duplicate line SHIFT + OPT + ↓ or ↑ ----------
console.log('Log one thing!')
console.log('Log another thing!')
//log another thing!

//----------Multi-select OPT + click, or OPT + ⌘ + ↑ or ↓----------
//can be used with other shortcuts like line/word skips
//add quotations around values
let doggo = 'dog';
let cat = 'cat';
let giraffe = 'giraffe';
let hippo = 'hippo';

//console.log each
console.log(definitions.doCoolstuff(1));
console.log(definitions.doCoolstuff(2));
console.log(definitions.doCoolstuff(3));

// ---------Comments----------
  // ----------toggle line comment ⌘ + '/' ---------
  // Lalalalalalaa
  //----------toggle block comment SHIFT + OPT + A ----------
  Loolooloo/* more loos! */looloo




//----------format document SHIFT + OPT + F----------
//ever get down this nested rabbit hole?

let silly = ['s', 'i', 'l', 'l', 'y']

silly.forEach((letter) => {
  if (letter = 'l') {
    let coolL = definitions.doCoolstuff(l);
    if (coolL !== 'actually cool') {
      (async cry() => {
        await console.log(🥲);
          return (function collectSelf = () {
          return 😮‍💨;
        })();
      });
  }
  }
})

//----------indent /outdent ⌘ + '[', ⌘ + ']' ----------
//do this above


//----------select line ⌘ + L ----------

let select = 'Select Me!';

//----------delete line ⌘ + SHIFT + K ----------


//---------Select all occurences of current selection  SHIFT + ⌘ + L ---------

//change giraffe to zebra

let safari = 'Look, another giraffe!';

//---------Go to line CTRL + G, enter in line---------

`Check out th animal on line 22!`

  //---------Go to symbol ⌘ + SHIFT + O, enter/select symbol---------

  `Where's the ${doggo}`

  //---------Change symbol definition F2---------
  `NOTE, DEPENDING ON MAC SETTINGS YOU MIGHT NEED TO ADD fn BEFORE HITTING F12`

  `Let's make ${doggo} a doggo`

  //---------Peak definition OPT + F12---------
  //---------Peak References SHIFT + F12---------
  `NOTE, DEPENDING ON MAC SETTINGS YOU MIGHT NEED TO ADD fn BEFORE HITTING F12`

//where was dog defined
console.log(doggo);


//---------Go to file ⌘ + P---------
//---------Go back / forward CTRL + '-' / SHIFT + CTRL + '-'  ---------
//---------Move tab to next editor group ⌘ + CTRL + Left/Right---------
//---------Go to editor screen  ⌘ + 1,2,3 etc. ---------
//---------Close file ⌘ + W  ---------

`Open definitions?`
  `Then come back`
  `Then go forward to definitions`
  `Then move to next editor screen`
  `Then come back to editor 1`
  `Then close definitions in screen 1`

//---------navigate tab history (in editor group) CTRL + SHIFT + Tab---------
//open gettysburg address
//move it to group 2
//go back to the index tab.

//---------Replace in files SHIFT + ⌘ + H---------
//search for 'stuff', replace with 'stuff

//---------open terminal CTRL + ` (backtick)---------
//---------open new terminal CTRL + SHIFT + ` (backtick)---------

//---------show current file in finder ⌘ + K then R---------

